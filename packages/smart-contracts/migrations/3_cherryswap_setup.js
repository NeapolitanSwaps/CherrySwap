var CherryDai = artifacts.require("./CherryDai.sol");
var CherrySwap = artifacts.require("./CherrySwap.sol");

module.exports = async (deployer, network, accounts) => {
    //const cherryDai = await CherryDai.deployed();
    //const cherrySwap = await CherrySwap.deployed();

    //await cherrySwap.setToken(cherryDai.address);
}
